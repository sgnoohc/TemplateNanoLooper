#include "Terminate_VBSHWW.h"

void Terminate_VBSHWW()
{
    //==============================================
    // Terminate_VBSHWW:
    // This function gets called after the event looping.
    // This is where one does stuff that needs to happen after the event looping for category VBSHWW.
    //==============================================

    // To place eventlist writing or something that needs to happen post loop.
    // TODO: implement event list writing
}
